div.ui.small.breadcrumb(ng-repeat="crumb in breadcrumbs")
    div(ng-class="{ active: $last }")
        a(ui-sref="{{ crumb.route }}", ng-if="!$last") {{ crumb.displayName }}
        span(ng-show="$last") {{ crumb.displayName }}
        div.divider(ng-if="!$last") &nbsp;/&nbsp;